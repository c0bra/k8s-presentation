version: '3'
services:
  app:
    build:
      context: .
    ports:
      - "3000:80"
    environment:
      REDIS_URL: redis://redis:6379
    networks:
      - mynetwork

  redis:
    image: redis:latest
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes
    volumes:
      - /tmp/redis:/data
    networks:
      - mynetwork

networks:
  mynetwork:
